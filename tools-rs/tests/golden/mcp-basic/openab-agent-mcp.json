{
  "mcpServers": {
    "test-stdio": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@example/mcp-server"
      ],
      "env": {
        "API_KEY": "${env:EXAMPLE_API_KEY}",
        "REGION": "us-east-1"
      }
    }
  }
}
