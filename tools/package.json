{
  "name": "agents-shared-capabilities-tools",
  "private": true,
  "version": "0.0.1",
  "description": "sync + lint tooling for the capability catalog",
  "scripts": {
    "lint": "node lint.js",
    "sync": "node sync.js"
  }
}
