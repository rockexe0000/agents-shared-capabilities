'use strict';
/*
 * codex projector (STUB — to be completed).
 * Target: ~/.codex/config.toml `[mcp_servers.<name>]` tables.
 * Emit intended TOML to .sync-output/codex.mcp.toml for reviewed merge until the
 * read-modify-write merge into config.toml is implemented.
 */
module.exports.projectMcp = function projectMcp(/* servers, home */) {
  throw new Error('codex MCP projector not implemented — see handoff');
};
