# 複製成 secrets/.env(gitignore),填入 registry 以 env: 參照的變數真值。
# 真值永不提交;升級成 keychain/1Password/Vault/SOPS 時只換 resolver,不動此清單。
WORKSPACE_DIR=/home/node/workspace
# octobroker (pod-local sidecar on 127.0.0.1:8079):
# OCTOBROKER_KEY=                     # X-Octobroker-Key (agent_key; from octobroker-keys secret)
# SOME_TOKEN=
