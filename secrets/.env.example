# 複製成 secrets/.env(gitignore),填入 registry 以 env: 參照的變數真值。
# 真值永不提交。此檔只服務 env:/${VAR} backend;其他 backend 不經此檔,由 CLI 於 sync/runtime 取:
#   env:VAR                     → 此檔 / process.env
#   ${VAR}                      → 同上(字串內插)
#   op://vault/item/field       → 1Password CLI(op read)
#   vault:<path>#<field>        → HashiCorp Vault(vault kv get -field=<field> <path>)
#   keychain:<service>[/<acct>] → macOS Keychain(security find-generic-password)
# 缺對應 CLI 或取值失敗 → 保留參照 + warn,絕不烘入真值。
WORKSPACE_DIR=/home/node/workspace
# octobroker (pod-local sidecar on 127.0.0.1:8079):
# OCTOBROKER_KEY=                     # X-Octobroker-Key (agent_key; from octobroker-keys secret)
# SOME_TOKEN=
